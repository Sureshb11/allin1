# Signing configuration
# Option A: set these properties here (NOT RECOMMENDED for real projects)
# Option B (recommended): create keystore.properties at project root and DO NOT commit it.
#
# Properties used when building signed release APK/AAB if keystore.properties does not exist.
# Example values (replace):
# MYAPP_UPLOAD_STORE_FILE=android/app/release.keystore
# MYAPP_UPLOAD_STORE_PASSWORD=your-store-password
# MYAPP_UPLOAD_KEY_ALIAS=allin1
# MYAPP_UPLOAD_KEY_PASSWORD=your-key-password
